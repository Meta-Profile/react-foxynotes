import styled from 'styled-components';
import { FlexBox } from '../ui/FlexBox';
import { Space } from '../ui/Box';

export const ProfileToolboxWrapper = styled(FlexBox)`
    gap: ${Space.small}px;
`;
