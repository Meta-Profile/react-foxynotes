export interface MetaProfileCategory {
    mpcId: number;
    title: string;
    icon: string;
}

export interface MetaProfileField {
    mpcId: number;
    mpfId: number;
    title: string;
}

export interface MFType {
    mftId: number;
    title: string;
    icon: string;
}
